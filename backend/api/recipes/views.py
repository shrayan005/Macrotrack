# FILE: api/recipes/views.py
# PURPOSE: Re-exporter for recipe-related views.
#          Recipe operations (including log-as-meal) are handled via
#          RecipeViewSet in api/recipes/viewset.py.
# USED BY: api/views.py (re-exporter), api/urls.py
# Recipe operations are handled entirely by RecipeViewSet.
# The log_to_meal action on RecipeViewSet handles the "log recipe to diary" flow.
# This module is a placeholder for future function-based views.
